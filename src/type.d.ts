export type menuType = {
  hideMenu: boolean;
  setHideMenu(param): void;
  infoType: string;
  setInfoType(param): void;
};

export type headerType = {
  hideMenu: boolean;
  setHideMenu(param): void;
};
